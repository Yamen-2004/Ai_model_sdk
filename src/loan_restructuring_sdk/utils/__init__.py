"""Small, dependency-free helpers shared across modules: exceptions, logging, date math.

Nothing in this package imports from any other SDK module -- it exists to be
imported *by* everything else without creating cycles.
"""
